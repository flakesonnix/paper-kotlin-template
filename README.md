# Paper Kotlin Template

Paper 1.26.2 — Kotlin 2.0.21 — Java 21.

```bash
nix develop
gradle shadowJar
# → build/libs/template-plugin-1.0.0.jar
```

HikariCP (sqlite/mysql/pg), Spotless/ktlint, nixfmt, IDEA, flake.

Use: `Use this template` → `gh repo create --template`.
